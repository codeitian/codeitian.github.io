
npm version

npm init
npm install uuid
npm install --save-dev nodemon or npm install -D nodemon /this is for the dependecies so I can delete nodemodules folder: i can recreate it by npm istall

call the file node index.js or just node index


////for reference folder when working on files
cd reference
node path pathdemo //the filename




//////
ctrl+c to stop server from running


/////// no refresh of localhost, package-json 
npm install nodemon
npm run dev
 "scripts": {
    "start": "node index",
    "dev": "nodemon index"
  }


  npm install dotenv

  "scripts": {
    "start": "node index3.js",
    "dev": "nodemon --env-file=.env index3.js"
  }

//git-scm.com
git init 
git add
git commit -m, 'Initial commit'
heroku create


  //heroku.com signup
  heroku CLI
  download and install